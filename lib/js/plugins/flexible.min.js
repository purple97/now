/**
 * Created by zhh on 14-12-4.
 * 10949221@qq.com
 */
!function (a) {
    var c, d, e, f = a.document, g = f.documentElement, h = f.querySelector('meta[name="viewport"]'), i = f.querySelector('meta[name="flexible"]'),av=a.navigator.appVersion;
    if (h) {
        console.warn("使用已有的meta标签.");
        return
    }
    if (!c && !d) {
        var k = (a.navigator.appVersion.match(/android/gi), a.navigator.appVersion.match(/iphone/gi)), c = a.devicePixelRatio;
        c = k ? c >= 3 ? 3 : c >= 2 ? 2 : 1 : 1, d = 1 / c;
    }
    g.setAttribute("data-devtype", av.match(/iphone/gi) ? "iphone" : "android");
    if (g.setAttribute("data-dpr", c), !h){
        if (h = f.createElement("meta"), h.setAttribute("name", "viewport"), h.setAttribute("content", "initial-scale=" + d + ", maximum-scale=" + d + ", minimum-scale=" + d + ", user-scalable=no"), g.firstElementChild)g.firstElementChild.appendChild(h);
        else {
            var l = f.createElement("div");
            l.appendChild(h), f.write(l.innerHTML);
        }}
}(window);